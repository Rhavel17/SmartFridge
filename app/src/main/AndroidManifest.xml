<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartfridge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartFridge">
        <activity android:name=".AddListActivity" android:parentActivityName=".ShoppingListActivity"></activity>
        <activity
            android:name=".PhotosActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ControlCenterActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MessagesActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RecipesActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FridgeContentsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ShoppingListActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>